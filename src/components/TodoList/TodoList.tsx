import React from "react"
// import { Todo } from "../index"


export const TodoList = () => {
    // const data = useFetch(process.env.URL)

    return (
        <>
            {/*{data.map(todo => <Todo {...todo}/>)}*/}
        </>
    )
}

